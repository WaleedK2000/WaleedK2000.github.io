import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  basePath: "/waleedk2000.github.io",
  assetPrefix: "/waleedk2000.github.io/",

  trailingSlash: true,
};

export default nextConfig;
